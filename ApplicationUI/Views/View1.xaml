<UserControl x:Class="ApplicationUI.Views.View1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ApplicationUI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- this data context is so the IDE helps us with the databinding. Remove it when done because it creates an instance of the view model.-->
    <UserControl.DataContext>
        <vm:View1ViewModel/>
    </UserControl.DataContext>
    <Grid Background="Red"/>

</UserControl>
